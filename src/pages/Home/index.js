import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Home = () => {
  return (
    <View style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}>
      <Text>Home Screen Afif</Text>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
